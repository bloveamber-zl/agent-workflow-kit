# Agent Workflow Evals

本目录用于保存目标项目级评估说明或本地评估输入。默认不要求每个目标项目都维护 eval case；通用 eval runner 和公共 case 留在 `agent-workflow-kit` 中。

如果目标项目需要沉淀专属评估，请记录：

- 用户输入或需求摘要
- 期望 intent、技术栈和 workflow 动作
- 期望生成或保留的文件
- 实际验证命令和结果
- 失败节点、根因和回归状态

不要在 eval 文件中保存密钥、账号、私有凭据或无关聊天内容。
