# 测试用例驱动流程

## 什么时候读

用户明确要求生成测试用例，或要求根据已完成需求生成测试并执行验证时读取。

## 启用条件

- 默认关闭，不主动提醒。
- 仅当用户明确要求“需要测试用例”或语义等价表达时，对当前需求启用。
- 启用范围仅限当前需求；只有用户明确取消时才退出。

## 运行模式

- `pre_implementation`：确认用例后先生成自动化测试，再实现功能并复测。
- `post_implementation`：根据原始需求生成用例，确认后直接测试现有实现。

## 需求级文件

启用后创建 `docs/test-cases/<requirement-id>.md`。每条用例包含：

- 用例 ID 和对应验收标准。
- 场景类型、前置条件、步骤和预期结果。
- 验证方式、自动化测试文件或人工验收路径。
- 状态、证据、不可自动化原因和剩余风险。

## 完成门禁

- 测试用例文档、开发文档和执行计划必须一起确认。
- 可自动化用例必须有可运行测试、真实断言和运行结果。
- 人工通过必须包含不可自动化原因、验收步骤和证据。
- 所有用例为“通过”或“人工通过”后，需求才能完成。
