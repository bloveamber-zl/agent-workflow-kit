# 测试与修复报告

## 什么时候读

需求进入验证阶段、测试失败、修复复测或判断是否可以标记 `passing` 时读取。

## 30 秒摘要

- 当前需求：REQ-WORKFLOW-BOOTSTRAP
- 当前测试状态：待运行
- 最近失败：暂无

## 测试记录

| 日期 | 需求 ID | 验收等级 | 增强触发 | 步骤 | 命令/路径 | 结果 | 证据类型 | 证据 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| {{CURRENT_DATE}} | REQ-WORKFLOW-BOOTSTRAP | L1 | 无：初始化验证 | step-01 | `./init.sh` | 待运行 | command | 待补充 | 首次验证 |

## 测试用例自验证

| 日期 | 需求 ID | 测试文件/用例 | 映射需求 | 自验证结果 | 说明 |
| --- | --- | --- | --- | --- | --- |
| - | - | - | - | 暂无 | 仅在新增或生成测试用例时填写 |

## 需求级测试用例执行

| 日期 | 需求 ID | 执行模式 | 用例 ID | 测试文件/人工路径 | 结果 | 降级原因 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| - | - | 未启用 | - | - | 暂无 | - | - |

## Patrol 自动化验收

| 日期 | 需求 ID | 用户选择 | Patrol 用例 | 覆盖验收点 | 命令/设备 | 结果 | 证据 | 未覆盖风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| - | - | 否 | - | - | - | 暂无 | - | 未选择 Patrol 或未配置 |

## 失败与修复循环

| ID | 关联需求 | 失败类型 | 失败现象 | 修复思路 | 复测命令 | 复测结果 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| - | - | - | 暂无 | - | - | - | - |

## 未覆盖风险

- 首次验证前，无法确认目标项目基础命令可用。
- E2E/Patrol/CI 未覆盖时，在这里记录原因、影响范围和后续补充计划。

## 规则

- 测试失败时不要把步骤标记为 `verified`。
- 测试失败时按 `docs/process/failure-taxonomy.md` 填写失败类型。
- 新增或生成测试用例时，先填写“测试用例自验证”。
- 用户选择 Patrol 时，填写“Patrol 自动化验收”，并记录测试文件、覆盖验收点、命令、设备和证据路径。
- 按需填写“增强触发”，说明启用或跳过需求澄清、TDD、诊断、架构巡检、E2E/Patrol、交接等增强能力的原因。
- 修复后必须重新运行原验证项，必要时增加覆盖失败的验证项。
- 通过证据要同步到 active plan 和 `docs/requirements/traceability.md`。
