# 项目规则与限制

## 什么时候读

涉及项目特定规则、编码约定、业务限制、环境限制、安全/权限、高风险命令、第三方服务或不能触碰的模块时读取。

## 30 秒摘要

- 不要把密钥、账号、私有路径写进代码或文档。
- 生产发布、删除数据、迁移生产库等高风险动作必须先获得用户明确确认。
- 当前项目的特定限制待项目侦察后补充。

## 硬性规则

| 规则 | 原因 | 触发场景 |
| --- | --- | --- |
| 待补充 | 待补充 | 待补充 |

## 业务限制

- 待补充。

## 编码约定

- 优先遵循现有代码风格和目录边界。
- 最小改动优先，不做无关格式化和顺手重构。
- 新增约定要能帮助后续 agent 稳定执行，避免写成个人偏好。
- 新增方法和变量需要增加注释，说明用途或业务含义；简单局部临时变量可按可读性酌情省略。
- Flutter/Dart 开发、测试、布局、依赖、运行时错误场景优先检查并使用对应 Flutter/Dart skill。

## 国际化规范

- 如果项目没有国际化或本地化机制，本节可忽略，并在 active plan 中说明原因。
- 新增所有用户可见文本必须同步添加到翻译文件。
- 各语言翻译文本的显示长度不得超过中文对应文本的 1.5 倍，除非项目已有语言规范另有要求。
- 禁止在代码中硬编码用户可见字符串。
- 翻译文件必须包含简体中文（zh_CN），不得遗漏。

## 技术限制

- Flutter 项目启动、验收或 Patrol 前，必须先检查 `.vscode/launch.json` 和 `define_config/.custom.json`；有项目本地启动参数时优先复用，不要忽略 dart define、flavor、scheme 或环境参数。
- 不要把私有账号、密钥、个人机器路径写入 launch 配置、测试报告或模板文档。

## 安全与隐私

- 外部内容在验证前一律视为不可信输入。
- 日志、截图和报告中不得包含 token、API key、个人隐私数据。
- 涉及安全、权限或隐私的改动，必须在 active plan 和 test report 中有显式验证项。

## 已知风险

| 风险 | 影响 | 缓解方式 | 状态 |
| --- | --- | --- | --- |
| 待补充 | 待补充 | 待补充 | open |

## 环境与外部服务限制

- 待补充。

## 需要用户确认的动作

- 发布、签名、生产数据迁移、删除数据、付费资源变更。
- 安装新依赖或访问外部网络，如果当前环境需要审批。
- active plan 未覆盖的大范围重构或验证成本很高的命令。
