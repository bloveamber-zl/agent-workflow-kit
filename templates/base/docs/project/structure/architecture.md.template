# 项目架构

## 什么时候读

当任务涉及路由、API、状态管理、数据模型、跨模块改动或架构边界时读取。

## 30 秒摘要

- 技术栈：`{{STACK_NAME}}`
- 分层模型：待项目侦察后补充。
- 关键入口：待项目侦察后补充。
- 高风险区域：记录在 `docs/project/constraints.md` 或当前 active plan。

## 领域地图

| 领域 | 负责什么 | 主要入口 | 相关规格 |
| --- | --- | --- | --- |
| 待补充 | 待补充 | 待补充 | 待补充 |

## 分层与依赖规则

- 低层不应依赖高层。
- UI 或入口层不应绕过 service/runtime/repo 等项目既有边界。
- 新增依赖、跨层访问或共享工具扩张，需要在对应 design doc 或 active plan 中说明理由。
- 共享 util 必须保持通用，不承载业务流程。

## 横切关注点

| 关注点 | 允许边界 | 备注 |
| --- | --- | --- |
| 日志 | 待补充 | 待补充 |
| 配置 | 待补充 | 待补充 |
| 外部 API | 待补充 | 待补充 |
| 权限/Auth | 待补充 | 待补充 |

## 变更检查

如果实现改变了领域地图、依赖方向、数据流或关键边界：

1. 更新本文件。
2. 更新相关 design doc。
3. 必要时在 `docs/project/constraints.md` 或当前 active plan 中记录新风险。
