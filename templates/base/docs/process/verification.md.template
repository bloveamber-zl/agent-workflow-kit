# 验证与验收工作流

这份文档承接 `AGENTS.md` 中的验证入口。目标是让每个需求都能根据自身风险自动选择验证方式，并在实现完成后调用对应验证模块；如果失败，则继续修复和复测，直到通过或记录 blocker。

项目实际命令、不能默认运行的高成本命令和当前验证事实写在 `docs/verification.md`。本文件只放通用流程。

## 推荐闭环

默认由主 Agent 串起完整闭环：

```text
需求解析 -> 开发文档 -> 执行计划确认 -> 实现 -> 验证模块 -> 独立复核门禁 -> 修复 -> 复测 -> 记录证据
```

主流程保持精简；增强能力只按场景触发。不要把需求澄清、PRD、拆票、TDD、诊断、架构巡检、E2E 或交接文档变成每个需求的固定步骤。

职责划分：

- 主实现流程：理解需求、更新 active plan、实现代码、运行验证、修复失败。
- 验证模块：根据需求内容运行单测、`./init.sh`、本地/模拟器验收或构建验证。
- 独立复核门禁：用 review 视角挑问题，重点检查需求遗漏、边界条件、UI 还原、真实环境风险和验证证据。
- 回修复测：发现问题后回到主实现流程做最小修复，再重新运行同一组验证和必要复核。

一般需求可以由主 Agent 自查完成复核。高风险需求应启用独立复核视角；当用户明确要求 Subagent，或当前环境支持并且任务边界清晰时，可使用独立 Subagent 做复核。

## 场景触发增强

以下增强能力是可选模块，只在触发条件成立时使用；未触发时，在 active plan、`docs/requirements/traceability.md` 或 `docs/reports/test-report.md` 写明跳过原因即可。

| 增强能力 | 触发条件 | 输出/记录 | 不触发时 |
| --- | --- | --- | --- |
| 需求澄清 | 需求目标、边界、术语、验收标准不清；用户描述和代码现状冲突 | open questions、领域术语、必要 ADR 或设计约束 | 需求清晰且可直接映射验收项时跳过 |
| PRD 整理 | 需求跨多个用户故事、多个模块或需要长期追踪 | PRD/开发文档摘要、用户故事、范围和非范围 | 小修、小 UI、单点 bug 不强制生成 |
| 任务拆分 | 需求无法在一个独立步骤内完成；存在依赖或可并行垂直切片 | active plan 中的 AFK/HITL 步骤、阻塞关系 | 单步骤可完成时保持一个步骤 |
| TDD/测试优先 | 纯逻辑、解析、状态机、复杂边界或 bugfix 有可测复现路径 | 先失败后通过的测试，或无法先红的原因 | 纯文档、简单样式、不可稳定复现时不强制 |
| 诊断 loop | 验证失败、性能回退、非确定性 bug、多轮修复仍失败 | 复现路径、假设、探针、修复和复测证据 | 首次通过或失败原因明显时不额外展开 |
| 架构巡检 | 跨模块大改、无合适测试缝、重复失败、明显耦合阻碍验证 | 架构风险、建议和是否进入技术债 | 普通需求不默认巡检 |
| E2E/Patrol | 用户选择自动化验收；L3/L4；或涉及权限、原生能力、核心链路、跨端用户路径 | 自动化用例、E2E/Patrol 命令、日志、截图或未覆盖风险 | 用户未选择且风险可由较轻验证覆盖时跳过 |
| 交接 | 长任务、上下文过多、阻塞等待、跨会话继续 | `docs/session-handoff.md` 或 active plan handoff | 短任务收尾不强制 |

使用增强能力前先问两个问题：

1. 这个需求不用该增强能力是否仍能可靠验收？
2. 该增强能力的输出是否会被后续实现、验证或交接实际使用？

如果两个问题答案都是“否”，不要触发，避免流程膨胀。

## 自动判定规则

创建或登记需求时，即使用户没有说明如何测试，也要根据需求内容在开发文档、active plan 和 `docs/requirements/traceability.md` 写入对应验证项。

需求实现完成后，必须按已登记的验证项调用对应验证模块：

- 基础验证模块：按 `docs/verification.md` 中的项目命令运行基础验证、严格验证或测试
- 局部测试模块：具体测试文件或针对性测试命令
- 本地/模拟器验收模块：`scripts/acceptance_simulator.sh`、`ACCEPTANCE_KEEP_RUNNING=1 scripts/acceptance_simulator.sh`
- 构建验证模块：按需查看平台构建文档后执行对应构建命令
- E2E 验收模块：按项目配置运行端到端测试；当前栈推荐工具为 `{{E2E_TOOL_NAME}}`，推荐命令为 `{{E2E_TEST_COMMAND}}`

### 验收等级

| 等级 | 场景 | 最低验证 | 证据 |
| --- | --- | --- | --- |
| L0 | 文档、注释、配置清单、小范围脚本说明 | 格式、JSON、diff 或轻量命令；可跳过启动验收但必须说明原因 | 命令输出或跳过原因 |
| L1 | 纯逻辑、解析、工具函数、状态计算 | 单测或局部测试 + 基础验证 | 测试文件、命令、结果 |
| L2 | 页面、布局、弹窗、文案、滚动、键盘、用户交互 | 基础验证 + 本地/模拟器启动验收，必要时截图或人工清单 | 日志、截图、人工结论 |
| L3 | 权限、相册、文件、录音、通知、定位、WebView、原生插件、平台配置 | 本地/模拟器验收；项目已配置 E2E 时优先补 E2E | 关键路径、设备、日志、截图、未覆盖风险 |
| L4 | 支付、账号、资产、核心链路、跨端、发布、数据迁移、安全隐私、多轮失败修复 | 必须考虑 E2E/CI/独立复核；无法覆盖时记录 blocker | E2E/CI 报告、复核结论、风险记录 |

自动判定时同时写明：

- 验收等级
- 判定依据：需求文字、代码 diff、技术栈、修改文件、历史失败
- 置信度：高 / 中 / 低
- 触发的增强能力和未触发原因
- 选择的验证命令
- 未覆盖风险和降级原因

默认判定：

- 纯逻辑、解析、工具函数、状态计算：优先补单测或局部测试，并运行对应测试、`./init.sh`。
- 接口字段、缓存、本地持久化、状态流转：补模型/解析/状态测试或可验证日志路径，并运行对应测试、`./init.sh`。
- 页面 UI、设计稿还原、文案、布局、弹窗、滚动、键盘、空状态：运行 `./init.sh`，并按风险运行 `scripts/acceptance_simulator.sh` 或 `ACCEPTANCE_KEEP_RUNNING=1 scripts/acceptance_simulator.sh` 做本地/模拟器验收。
- 权限、相册、文件选择、录音、播放、支付、原生插件、平台配置：必须做本地/模拟器验收；如果只靠本地/模拟器不能证明原生改动，再考虑平台构建，并说明原因。
- 纯文档、注释、任务清单：可跳过本地/模拟器验收，但要说明跳过原因，并至少运行格式、JSON、diff 类轻量验证。

## 可选测试用例驱动模式

测试用例驱动模式默认关闭且不主动提醒。只有用户明确要求“需要测试用例”、要求先生成测试用例，或要求根据已完成需求生成测试并执行验证时，才对当前需求启用。

- `pre_implementation`：先生成 `docs/test-cases/<requirement-id>.md`，与开发文档和执行计划一起确认，再生成自动化测试、验证真实红灯、实现和复测。
- `post_implementation`：先根据原始需求和现有实现生成测试用例，确认后直接生成并运行测试；不能根据现有代码反向削弱验收目标，也不能伪造先红证据。
- 无法自动化的用例必须记录原因、步骤、环境、证据和剩余风险，人工验收后可标记为“人工通过”。
- 每个验收标准必须映射到测试用例 ID；所有用例通过或人工通过后，需求才能完成。

详细格式和门禁见 `docs/test-cases/README.md`。

## 可选 Patrol 自动化验收

Flutter 项目如果已生成 `docs/testing/patrol.md` 和 `scripts/patrol_acceptance.sh`，在开发完成并通过基础验证后，可以由用户显式选择 Patrol 验收。

触发方式：

- 用户明确要求：“用 Patrol 验证”“生成 Patrol 用例”“跑端到端自动化”。
- L3/L4 高风险需求，Agent 建议使用 Patrol，用户确认后执行。
- 需求涉及页面关键交互、系统权限、支付、订阅、账号、资产、跨 App 或核心链路，且普通本地/模拟器验收不足以证明结果。

选择 Patrol 后，Agent 必须执行以下步骤：

1. 从需求文档和验收标准提取可自动化验收点。
2. 在 active plan 和 `docs/requirements/traceability.md` 记录 Patrol 触发原因、目标用例文件和预期覆盖点。
3. 检查目标页面是否有稳定定位点；没有时先补最小必要 `ValueKey` 或项目既有定位方式。
4. 生成或更新 `patrol_test/<requirement-id>_test.dart`，文件名使用小写短横线或下划线，避免覆盖无关用例。
5. 用例必须包含真实断言：元素存在、状态正确、点击跳转、弹窗出现、权限或系统交互结果等；不能只启动 App。
6. 若需要登录态、mock 数据、测试账号、后端环境、设备权限或特定构建参数，先写入用例说明、active plan 或 `docs/testing/patrol.md` 的项目配置区。
7. 先做测试用例自验证，再运行 `scripts/patrol_acceptance.sh`；必要时通过 `PATROL_TARGET` 指定本需求用例。
8. 将 `summary.md`、`evidence.json`、日志、截图或 blocker 写回 active plan、`docs/requirements/traceability.md` 和 `docs/reports/test-report.md`。

不得把以下情况标记为 Patrol 已覆盖：

- 只新增了冒烟测试或只验证 App 启动。
- 用例没有映射到需求 ID 或验收标准。
- 由于缺少 CLI、设备、签名、账号、权限、后端环境、稳定定位点或构建参数导致未运行。
- 运行失败后没有完成修复复测。

如果用户未选择 Patrol，或当前项目未配置 Patrol，必须记录跳过原因和剩余风险；必要时退回 `scripts/acceptance_simulator.sh`、局部测试或人工复核。

## 测试用例自验证

当 Agent 根据需求文档、需求文字或未提交代码生成测试用例时，必须先验证测试用例本身，再用它验证代码。

最低检查：

- 测试能运行：语法正确、依赖可解析、命令可执行。
- 测试有断言：不能只启动、不报错或只检查 mock 调用。
- 测试映射需求：每个新增测试要能对应需求 ID、验收标准或失败复现路径。
- 测试可回归：测试名说明意图，失败信息能定位问题，后续可重复运行。

Bugfix 场景优先确认“修复前失败、修复后通过”。如果无法在修复前版本验证，必须在 `docs/reports/test-report.md` 写明原因，并由复核视角检查断言是否覆盖真实失败。

## 独立复核门禁

以下需求完成后，必须进入独立复核门禁：

- 设计稿还原、复杂页面、弹窗、键盘、滚动、空状态。
- 权限、相册、文件选择、录音、播放、支付。
- 原生插件、平台配置、构建脚本。
- 涉及扣费、资产、钱包、账号状态、消息发送等高影响用户行为。
- 修改范围跨多个模块，或验证失败后已经修复过一轮。

复核要站在“找问题”的角度，不重复实现者的证明路径。检查清单：

- 需求是否完整覆盖，有没有只实现了局部场景。
- 验证项是否匹配需求风险，有没有漏跑本地/模拟器验收或局部测试。
- UI 是否存在设计稿偏差、溢出、遮挡、安全区、长文案、空状态问题。
- 异常、取消、无权限、接口缺字段、重复点击、异步返回后页面销毁等边界是否处理。
- 证据是否足以支持 `passing`，包括命令、日志、验收产物或人工结论。

复核发现问题时：

1. 在 `docs/coding-progress.md` 或当前任务证据中记录问题和复现路径。
2. 回到主实现流程做最小修复。
3. 重新运行原验证项，以及覆盖该问题的新增验证项。
4. 再次进入复核；通过后才能标记 `passing`。

## 本地/模拟器验收

- 验收入口：`scripts/acceptance_simulator.sh`
- 使用说明：`docs/acceptance_simulator.md`

常用命令：

```bash
# 只检查命令生成和产物路径
ACCEPTANCE_DRY_RUN=1 scripts/acceptance_simulator.sh

# 启动并自动结束
scripts/acceptance_simulator.sh

# 启动后保持运行，方便人工点测
ACCEPTANCE_KEEP_RUNNING=1 scripts/acceptance_simulator.sh

# 自定义验收启动命令
ACCEPTANCE_COMMAND='<command>' scripts/acceptance_simulator.sh
```

涉及页面展示、权限、相册、文件选择、录音、支付页等用户可见交互时，按风险运行本地/模拟器验收，并把 `summary.md`、`manual-checklist.md` 或人工结论记录到 active plan、`docs/requirements/traceability.md` 或 `docs/reports/test-report.md`。

本地/模拟器验收可能触发平台编译、设备访问或权限访问；不要作为每次轻量验证的默认步骤，按需求风险执行。

## 失败处理

验证失败时不要把功能标记为 `passing`。

1. 读取失败命令的完整输出。
2. 按 `docs/process/failure-taxonomy.md` 选择失败类型。
3. 根据失败日志、截图或模拟器现象定位最小问题。
4. 做最小修复，不扩大到无关功能。
5. 重新运行同一组验证。
6. 若继续失败，重复定位和修复。
7. 若因外部服务、账号、权限或环境无法继续，记录为 blocker，并把 `.agent/state/current-task.json` 状态设为 `blocked`。

blocker 记录必须包含：

- 失败命令
- 失败类型
- 失败现象
- 已尝试步骤
- 下一步需要的账号、权限、环境或外部信息

## 证据记录

功能只有在目标行为实现、验证通过、证据写回后，才能标记为 `passing`。

证据至少写入以下位置：

- 当前 active plan 的步骤证据
- `docs/requirements/traceability.md`
- `docs/reports/test-report.md`

`docs/feature_list.json` 只保留项目级状态摘要，`docs/coding-progress.md` 只保留会话级进度摘要。

页面或系统交互验收建议记录：

```text
{{CURRENT_DATE}}: 运行 ACCEPTANCE_KEEP_RUNNING=1 scripts/acceptance_simulator.sh，
进入 xxx 页面，对照需求检查布局、文案和交互状态；
产物目录 .dart_tool/acceptance/xxxx；结论：通过。
```
