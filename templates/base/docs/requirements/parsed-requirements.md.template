# 解析后的需求文档

## 什么时候读

用户提供 Word、PDF 或文字需求后读取和更新。本文件保存结构化需求，不保留原始需求文件。

## 30 秒摘要

- 当前需求来源：待补充。
- 当前需求版本：v0，待确认。
- 当前状态：等待需求解析或首次确认。
- 待确认问题：见 `docs/requirements/open-questions.md`。

## 需求条目

| ID | 标题 | 类型 | 优先级 | 状态 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| REQ-WORKFLOW-BOOTSTRAP | 建立 agent 持久化工作流 | workflow | P0 | blocked | `./init.sh` 通过，文档和计划可续跑 |

## 用户流程

1. 用户提供需求文档或文字描述。
2. Agent 解析需求并生成结构化需求文档。
3. Agent 结合项目现状生成开发文档。
4. 用户确认开发文档和执行计划。
5. Agent 按 active plan 开发、验证、修复和更新状态。

## 非功能需求

- 可续跑：下一轮会话必须能通过 repo-local 文档恢复状态。
- 可验证：每个需求条目必须能追踪到验证项和证据。
- 可审计：需求疑问、假设、失败修复和复测结果必须记录。

## 不做什么

- 不保留原始 Word/PDF 文件。
- 不在需求未澄清或计划未确认时直接开发。

## 更新规则

- 新需求或需求变更先更新本文件，再更新 `docs/requirements/traceability.md` 和 `docs/feature_list.json`。
- 不确定内容写入 `docs/requirements/open-questions.md`，不要直接当成事实。
