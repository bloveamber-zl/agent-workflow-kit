# 项目验证入口与命令

## 什么时候读

准备运行验证、选择测试范围、记录验收证据或判断能否标记步骤 `verified` 时读取。

通用验证流程、风险判定、独立复核和失败复测规则见 `docs/process/verification.md`。

## 30 秒摘要

- 基础验证：`./init.sh`
- 严格验证：`VERIFY_MODE=analyze ./init.sh`
- 测试验证：`VERIFY_MODE=test ./init.sh`
- 启动验证：`RUN_START_COMMAND=1 ./init.sh`
- 本地/模拟器验收：`scripts/acceptance_simulator.sh`

## 当前命令

| 类型 | 命令 | 说明 |
| --- | --- | --- |
| 基础验证 | `{{PRIMARY_VERIFY_COMMAND}}` | 默认轻量验证 |
| 严格验证 | `{{STRICT_VERIFY_COMMAND}}` | 分析或更严格检查 |
| 测试 | `{{TEST_COMMAND}}` | 自动测试 |
| 启动 | `{{START_COMMAND}}` | 应用或服务启动 |
| 依赖同步 | `{{DEPENDENCY_SYNC_COMMAND}}` | 需要时手动开启 |

## 不能默认运行的命令

- 完整发布、签名、生产迁移、删除数据、付费资源变更。
- 成本高或需要账号权限的构建命令，除非用户明确要求。

## 项目特殊注意

- 需求未说明验证方式时，先按 `docs/process/verification.md` 判定验证项，再写入 active plan。
- 涉及 UI、交互、权限、文件、录音、支付或原生能力时，仅基础验证通常不足以证明完成。
- 高风险需求完成后，按 `docs/process/verification.md` 的独立复核门禁执行。
- 自动判定验收等级 L0-L4，并记录判定依据、置信度、验证命令和未覆盖风险。
- 增强能力按场景触发，不作为每个需求的固定步骤；触发或跳过原因要记录。
- 如果本轮生成或新增测试用例，先做测试用例自验证，再用测试验证代码。

## E2E / Patrol

- 当前栈推荐 E2E 工具：`{{E2E_TOOL_NAME}}`
- 推荐 E2E 命令：`{{E2E_TEST_COMMAND}}`
- 说明：{{E2E_SETUP_NOTE}}

Flutter 项目如果已生成 `docs/testing/patrol.md`，L3/L4 场景优先读取该文档。未配置 Patrol 时，不要把 Patrol 标记为已覆盖；应记录未覆盖原因，并退回本地/模拟器验收。

## 证据记录位置

- 当前 active plan 的步骤证据。
- `docs/requirements/traceability.md`。
- `docs/reports/test-report.md`。
- 必要时补充到 `docs/coding-progress.md`。
