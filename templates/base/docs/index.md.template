# {{PROJECT_NAME}} 文档地图

这份文件是渐进披露入口。开工先读它，再按当前任务状态读取必要文档；不要每次把所有文档都读一遍。

## 开工必读

1. `AGENTS.md`：根指令和硬规则入口。
2. `.agent/state/current-task.json`：机器可读的当前任务状态、关联需求、计划和 blocker。
3. `.agent/config.json`：机器可读的 workflow 版本、技术栈、trace 开关和覆盖策略。
4. `docs/coding-progress.md`：最近会话状态、当前需求、当前计划和下一步。
5. `docs/feature_list.json`：项目级需求索引，定位需求文档、开发文档和 active plan。
6. `docs/exec-plans/active/`：如果存在当前计划，读取对应计划并按当前步骤续跑。

## 按场景读取

| 场景 | 读取 |
| --- | --- |
| 用户提供新需求 | `docs/requirements/parsed-requirements.md`、`docs/requirements/open-questions.md` |
| 需要了解项目 | `docs/project/structure/overview.md` |
| 需要了解功能模块 | `docs/project/features/overview.md` |
| 要改代码 | `docs/project/constraints.md`、当前 active plan |
| 涉及架构/API/状态/数据模型 | `docs/project/structure/architecture.md`、相关 design doc |
| 涉及项目限制、安全、权限或高风险动作 | `docs/project/constraints.md` |
| 要测试或验收 | `docs/verification.md`、`docs/process/verification.md`、`docs/reports/test-report.md` |
| 验证失败或任务阻塞 | `docs/process/failure-taxonomy.md`、`docs/reports/test-report.md`、当前 active plan |
| Agent 链路输出与预期不一致 | `.agent/traces/`、`docs/process/badcase-analysis.md`、`docs/reports/eval-report.md` |
| 要检查是否漏需求 | `docs/requirements/traceability.md` |
| 前端/UI 改动 | `docs/project/frontend.md` |
| 已配置 CodeGraph 且要做影响分析 | `docs/tools/codegraph.md` |
| 用户明确要求 Open Design 生成设计稿或按设计稿生成代码 | `docs/tools/opendesign.md` |
| 需要交接 | `.agent/state/current-task.json`、`docs/session-handoff.md` |

## 文档职责

- `.agent/state/current-task.json`：当前任务状态，只保存机器可读摘要。
- `.agent/config.json`：workflow 版本、技术栈、trace 开关和覆盖策略。
- `.agent/traces/`：长任务、Runtime 节点和失败复盘的轻量事实记录。
- `.agent/evals/`：目标项目级评估说明或本地评估输入。
- `docs/project/structure/`：项目结构、架构、模块划分和目录说明。
- `docs/project/features/`：长期维护的功能模块、业务逻辑和使用说明。
- `docs/project/constraints.md`：项目特定规则、限制、编码约定和风险。
- `docs/project/frontend.md`：前端/UI 规则。
- `docs/requirements/`：解析后的需求、疑问、假设和需求追踪矩阵。
- `docs/design/`：结合项目现状后的开发文档，回答“准备怎么做”。
- `docs/exec-plans/active/`：当前执行计划，记录步骤、状态、验证和证据。
- `docs/exec-plans/completed/`：已完成计划归档。
- `docs/reports/`：测试报告、eval 报告、失败修复和复测记录。
- `docs/process/failure-taxonomy.md`：失败归因分类。
- `docs/process/badcase-analysis.md`：Agent 链路 badcase 定位和记录流程。
- `docs/tools/codegraph.md`：CodeGraph 可选增强说明，仅在初始化或升级时选择启用后生成。
- `docs/tools/opendesign.md`：Open Design 可选增强说明，仅在初始化或升级时选择启用后生成，并且只能由用户明确触发。

## 标准命令

项目验证入口见 `docs/verification.md`，完整验证策略见 `docs/process/verification.md`。

```bash
# 基础验证
./init.sh

# 严格验证
VERIFY_MODE=analyze ./init.sh

# 测试
VERIFY_MODE=test ./init.sh

# 同步依赖后再验证
SYNC_DEPS=1 ./init.sh

# 启动应用或服务
RUN_START_COMMAND=1 ./init.sh

# 本地/模拟器验收
scripts/acceptance_simulator.sh
```

## 需求阶段

- `intake`：已接收需求，等待解析。
- `clarifying`：存在待确认问题。
- `designing`：正在生成或确认开发文档。
- `planned`：执行计划已生成，等待确认或开工。
- `in_progress`：当前正在开发。
- `verifying`：实现完成，正在按需求测试。
- `blocked`：因已记录 blocker 无法继续。
- `passing`：需求实现、验证和证据均已完成。

同一时间只能有一个需求处于 `in_progress`，详细步骤状态写在 active plan 中。

## 收尾要求

结束会话前至少更新：

- `docs/coding-progress.md`
- `docs/feature_list.json`
- 当前 `docs/exec-plans/active/*.md`

如果涉及需求、验证或长会话，额外更新：

- `docs/requirements/traceability.md`
- `docs/reports/test-report.md`
- `docs/session-handoff.md`

如果提交 Git，提交信息用中文，格式示例：

```text
feat: 实现用户可见功能

需求：说明用户请求或问题。
实现：说明核心改法。
验证：说明实际运行过的命令。
```
