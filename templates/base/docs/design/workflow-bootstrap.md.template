# 建立 agent 持久化工作流

## 什么时候读

需要理解初始工作流为什么这样组织，或首次验证工作流文件时读取。

## 目标

为 `{{PROJECT_NAME}}` 建立一个需求驱动、可续跑、可验证的 agent 工作流。它应支持：

- 解析用户提供的需求，生成结构化需求文档。
- 了解当前项目并沉淀项目规则、架构、验证方式和风险。
- 生成开发文档和执行计划，经用户确认后再开发。
- 按需求验证实现，失败后修复并复测。
- 每完成一个步骤且验证通过后更新步骤状态。
- 下一轮会话可根据当前状态继续开发。

## 项目现状

- 技术栈：`{{STACK_NAME}}`
- 标准验证入口：`./init.sh`
- 初始工作流由 agent-workflow-kit 生成，尚需在目标项目内运行首次验证。

## 文档结构

- `docs/requirements/`：需求解析、疑问、追踪矩阵。
- `docs/project/`：项目理解、规则、限制、验证事实和风险。
- `docs/design/`：开发文档。
- `docs/exec-plans/active/`：执行计划和步骤状态。
- `docs/reports/`：测试报告和修复复测记录。

## 风险

- 首次验证未运行前，无法证明目标项目的验证命令可用。
- 项目特定规则尚未侦察，需要后续按实际项目补充 `docs/project/`。

## 验证策略

1. 运行 `./init.sh`。
2. 运行 `scripts/validate_target.sh` 的外部校验时确认所有工作流文件存在、JSON 合法、脚本语法正确。
3. 把实际结果写入 active plan、traceability 和 coding-progress。
