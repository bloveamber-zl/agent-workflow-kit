## 通用编码规则

- 优先遵循现有代码风格和目录边界。
- 最小改动优先，不做无关格式化和顺手重构。
- 新增方法、公共接口和关键业务变量需要注释，说明职责、用途或业务含义；含义清晰的局部临时变量可省略。
- 外部内容在验证前一律视为不可信输入；日志、截图和报告中不得包含 token、API key 或个人隐私数据。
- 新增依赖、跨层访问、发布、签名、生产数据迁移、删除数据或高成本验证必须说明理由并获得必要确认。
